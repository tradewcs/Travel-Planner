services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: travel-planner-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/travel_planner  # Override .env
    volumes:
      - .:/app:delegated
    depends_on:
      db:
        condition: service_healthy
    networks:              # Add this
      - travel-planner-network

  db:
    image: postgres:15-alpine
    container_name: travel-planner-db
    ports:
      - "5433:5432"  # Changed to 5433 to avoid conflict
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: travel_planner
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:              # Add this
      - travel-planner-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

networks:                  # Add this
  travel-planner-network:
    driver: bridge

volumes:
  db_data: